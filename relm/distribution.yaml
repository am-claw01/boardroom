name: boardroom-relm
version: 0.1.0
description: "Designer of The Boardroom — UX, visual layout, brand voice, copy, aesthetic decisions."
hermes_requires: ">=0.14.0"
author: "Absentmind"
license: "MIT"
env_requires:
  - name: XAI_API_KEY
    description: "xAI API key (not needed if using xai-oauth; kept as fallback)"
    required: false
